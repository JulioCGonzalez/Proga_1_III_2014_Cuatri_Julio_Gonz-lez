package quiz;

/**
 *
 * @author Julio César
 */
public class clsquiz {

//    public String addletras(String letra) {
//        char[] palabra = letra.toCharArray();
//        for (int i = 0; i < palabra.length; i++) {
//            if (letra.codePointAt(i) >= 65 && letra.codePointAt(i) >= 90) {
//                palabra[i] = Character.toLowerCase(palabra[i]);
//            } else {
//                if (letra.codePointAt(i) >= 97 && letra.codePointAt(i) >= 122) {
//                    palabra[i] = Character.toUpperCase(palabra[i]);
//                }
//            }
//
//        }
//        return String.valueOf(palabra);
//    }

    public void Acomodar(char[] palabra){
    char palabras[]=new char[5];
    String extra;
    String space="";
        for (int i = 0; i < palabras.length; i++) {
            palabras[i]=Character.toLowerCase(palabra[i]);
        }
    }
    
    
}
